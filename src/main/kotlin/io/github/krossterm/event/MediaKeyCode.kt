package io.github.krossterm.event

/** Media keys (play / pause / volume / etc.). */
public enum class MediaKeyCode {
    Play, Pause, PlayPause, Reverse, Stop,
    FastForward, Rewind, TrackNext, TrackPrevious, Record,
    LowerVolume, RaiseVolume, MuteVolume,
}
